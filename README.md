# 📝 To-Do List Fullstack API

Este é um projeto Fullstack de gerenciamento de tarefas, desenvolvido para explorar arquiteturas modernas, segurança com JWT e boas práticas de integração entre um backend **Java (Spring Boot)** e um frontend **React**.

## 🚀 Tecnologias Utilizadas

### Backend (`/server`)
- **Java 21** (LTS)
- **Spring Boot 3.4**
- **Spring Security** (Autenticação Stateless com JWT)
- **Spring Data JPA** & **Hibernate**
- **Flyway** (Gerenciamento de Migrations de Banco de Dados)
- **PostgreSQL** (Banco de Dados)
- **Dotenv** (Gerenciamento de variáveis de ambiente)
- **Maven** (Gerenciador de dependências)

### Frontend (`/frontend`)
- **React**
- **Vite** (Build tool rápida)
- **TypeScript** (Tipagem estática)
- **PNPM** (Gerenciador de pacotes)

---

## 📂 Estrutura do Projeto

O projeto está organizado em duas pastas principais:

```bash
todo-api/
├── server/       # API Backend (Spring Boot)
│   ├── src/
│   ├── .env      # Variáveis de ambiente do backend
│   └── pom.xml
│
└── frontend/     # Interface Frontend (React + Vite)
    ├── src/
    └── package.json
```
---
